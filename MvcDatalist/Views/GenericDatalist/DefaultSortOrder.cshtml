@{
    ViewBag.Title = "GenericDatalist.DefaultSortOrder";
}

<div class="panel panel-default">
    <div class="panel-heading">DefaultSortOrder</div>
    <div class="panel-body">
        <strong>Description</strong>
        <pre>Specifies default datalist column sort order, unlesss explicit sorting order is specified.</pre>
        <strong>Defaults</strong>
        <pre><a href="/Datalist/API/DatalistSortOrder">DatalistSortOrder</a>.<a href="/Datalist/DatalistSortOrder/Asc">Asc</a></pre>
        <strong>Remarks</strong>
        <pre>Default sort order will be used on next datalist data table load, unless user sort column explicitly.
TODO: autocomplete</pre>
        <strong>Examples</strong>
        <pre>
<strong>Columns:</strong> { <span class="str">"FirstName"</span>, <span class="str">"FirstName"</span> }, { <span class="str">"LastName"</span>, <span class="str">"LastName"</span> }, { <span class="str">"DateOfBirth"</span>, <span class="str">"DateOfBirth"</span> }, { <span class="str">"Account.LoginName"</span>, <span class="str">"LoginName"</span> }
<strong>DefaultSortOrder:</strong> <span class="cls">DatalistSortOrder</span>.Asc

Datalist data will be sorted by first column in Ascending order.</pre>
        @Html.Datalist("DefaultSortOrder1", null, new DefaultDatalist())
        <div class="separator"></div>
        <pre>
<strong>Columns:</strong> { <span class="str">"FirstName"</span>, <span class="str">"FirstName"</span> }, { <span class="str">"LastName"</span>, <span class="str">"LastName"</span> }, { <span class="str">"DateOfBirth"</span>, <span class="str">"DateOfBirth"</span> }, { <span class="str">"Account.LoginName"</span>, <span class="str">"LoginName"</span> }
<strong>DefaultSortOrder:</strong> <span class="cls">DatalistSortOrder</span>.Desc

Datalist data will be sorted by first column in Descending order.

<span class="kwrd">public</span> ExampleDatalist()
{
    DefaultSortOrder = <span class="cls">DatalistSortOrder</span>.Desc;
}
</pre>
        @Html.Datalist("DefaultSortColumn2", null, new ExampleDatalist())
    </div>
</div>